@{
    ViewData["Title"] = "Accesso negato";
}

<div class="text-center">
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">Non hai i permessi per accedere a questa risorsa.</p>
    <a asp-controller="Studenti" asp-action="Index" class="btn btn-primary">Torna alla Home</a>
</div>

// Views/Account/Lockout.cshtml
@{
    ViewData["Title"] = "Account bloccato";
}

<div class="text-center">
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">Questo account è stato temporaneamente bloccato. Riprova più tardi.</p>
    <a asp-controller="Studenti" asp-action="Index" class="btn btn-primary">Torna alla Home</a>
</div>